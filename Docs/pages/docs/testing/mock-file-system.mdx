---
sidebar_position: 1
title: MockFileSystem
---

# MockFileSystem

`MockFileSystem` is the in-memory implementation of `IFileSystem` shipped in `Testably.Abstractions.Testing`. It implements every member of `IFileSystem` and is exercised by the same test suite as `RealFileSystem` — if a test passes against the real file system, it passes against the mock, and vice versa.

```csharp
IFileSystem fileSystem = new MockFileSystem();

fileSystem.File.WriteAllText("hello.txt", "world");
string content = fileSystem.File.ReadAllText("hello.txt"); // "world"
```

The remaining pages in this section cover the most common configuration knobs:

- [Initialization](./initialization) — fluent and declarative ways to seed the file system.
- [Simulating other operating systems](./simulating-os) — case sensitivity, separators and drive layouts.
- [Drive management](./drive-management) — multiple drives and quotas.
- [FileSystemWatcher](./file-system-watcher) — events and the OS-specific event tables.
- [Intercept & Notify](./intercept-and-notify) — react to or block file system events from tests.
